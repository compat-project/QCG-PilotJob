{
	"request": "submit",
	"jobs": [ {
		"name": "env_${rcnt}",
		"execution": {
		  "exec": "/bin/bash",
		  "args": [ "-c", "/home/plgrid/plgkopta/qcg-pilotmanager/qcg-pilotmanager/script-env.sh" ],
		  "wd": "tmp/env-${rcnt}-${uniq}",
		  "stdout": "env.stdout",
		  "stderr": "env.stderr"
		},
		"resources": {
		  "numCores": {
				"exact": 1
		  }
		}
	} ]
}

